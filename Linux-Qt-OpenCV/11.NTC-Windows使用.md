


> NTC-Windows使用

## `Widget_Cam_Offline.h`（离线检测界面）

```cpp
#ifdef NTC
	CNTCClient client;
#endif
````

````cpp
#ifdef NTC
	void  NTC_Connect(); //连接
	void  NTC_DisConnect(); //断开
	void  SendState(); //发送长度,相机灯状态
	void  SendLockState(bool  bLocked); //发送模板编辑状态
	void  NTC_SendToStr(NTC_MSG,QString); //指定信号String字符串通信
	void  NTC_SendToCom(QString  sData); //发送串口
	static  int  ntcFunCallbackClient(char  *p, UINT  uLen);
	virtual  bool  nativeEvent(const  QByteArray  &eventType, void  *message, long  *result); // 处理Windows PostMessage消息
	void  NTC_Camera_Init(); //初始化完成
	void  NTC_Detect_Shutdown(DetectShutdown); //检测停机参数
#endif
````
##  `Widget_Cam_Offline.cpp`
````cpp
switch (ntcData.msgtype)
{
	case NTC_MSG_START: //收到启动信号
	..........
	case NTC_MSG_PAUSE: //收到暂停信号
	......
	case NTC_MSG_RESET: //收到暂停后,启动,继续检测
	.........
	case NTC_MSG_COILCUT: //换卷
	........
	case NTC_MSG_STOP: //停止
	........
	case NTC_MSG_SENT: //接收到换产品型号或检测幅宽消息
	........
	case NTC_MSG_MCU: // 控制器加密指令
	........
	case NTC_MSG_DENSE_DEFECT: // 密集缺陷
	......
	case NTC_MSG_EIP_SET_STATU: // 总体接收控制台EIP指令，现在只用来控制低速不检测
	.....
	case NTC_MSG_LOGIN: // 登录
	......
	case NTC_MSG_HEARTBEAT: // 心跳
	........
	case NTC_MSG_RETEST: // 缺陷复测
	
}
````

`````cpp
bool  Widget_Cam_Offline::nativeEvent(const  QByteArray  &eventType, void  *message, long  *result)
{
	Q_UNUSED(result);
	Q_UNUSED(eventType);
	QString sCurrentTime;
	sCurrentTime =  QDateTime::currentDateTime().toString("yyyy-MM-dd hh:mm:ss");
	MSG* msg = (MSG*)message;
	if (eventType ==  "windows_generic_MSG"  &&  msg->message  ==  WM_NTCMESSAGE)
}
`````


## `MainWindow`  中的 NTC 使用
### mainwindow.h
-   L180: MCU 版本号，受  `NTC_MSG_MCU`  消息更新
```cpp
QString m_sMCU_Version  =  "2026-03-16"; //控制器版本号，默认不能在线调踢废，除非收到NTC_MCU消息
```
-   L397:  `sig_NtcThread_Start()`  信号
```cpp
void  sig_NtcThread_Start(); //NTC发数据线程
```
-   L425:  `SendToCom()`  — 向串口发消息，分 NTC 发和串口发两种路径
```cpp
void  SendToCom(int  iCamid,QString  sData); //向串口发消息, 分NTC发,串口发 i为相机ID
```

### mainwindow.cpp
NTC 状态调试菜单，发送 `NTC_MSG_STATUS`
```cpp
//NTC状态调试
connect(ui->actionNTC, &QAction::triggered,[=](){
NTCData  ntcMsgSent;
memset(&ntcMsgSent, 0, sizeof(ntcMsgSent));
ntcMsgSent.msgtype  =  NTC_MSG_STATUS;
ntcMsgSent.clientid  =  11;
StateInfo stateInfo;
memset(&stateInfo, 0, sizeof(StateInfo));
stateInfo.fSpeed  =  120;
stateInfo.fLength=  100;

//相机状态灯
QString sState =  nullptr;
for (int  i  =  0; i  <  p_Mainwindow->m_iCamCount; i++)
{
sState+=QString::number(1);
}
strcpy_s(stateInfo.CamState, sState.toLatin1().data());
//Led灯
for(int  i=  0;i  <  MaxCamNum; i++)
{
stateInfo.CamLed[i] =  100;
}

//控制台为自动寻边
if(p_Mainwindow->m_iMeasureWidth  ==  1)
{
stateInfo.CoilEdge  =  p_Mainwindow->m_fEdge;
stateInfo.DetectWidth  =  p_Mainwindow->m_fWidth;//检测幅宽
stateInfo.ProductWidth  =  p_Mainwindow->m_fProduct_Width;//产品幅宽
}

//4组相机的边缘坐标值
for (int  i=0; i<p_Mainwindow->m_iCamCount; i++)
{
if (i  ==  0) stateInfo.GroupEdge[0] =  p_Mainwindow->m_fX_Edge_Cam1;
else  if (i  ==  p_Mainwindow->m_i2ndStartID  -  11) stateInfo.GroupEdge[1] =  p_Mainwindow->m_fX_Edge_Cam2;
else  if (i  ==  p_Mainwindow->m_i3rdStartID  -  11) stateInfo.GroupEdge[2] =  p_Mainwindow->m_fX_Edge_Cam3;
else  if (i  ==  p_Mainwindow->m_i4thStartID  -  11) stateInfo.GroupEdge[3] =  p_Mainwindow->m_fX_Edge_Cam4;
}
memcpy(ntcMsgSent.pData,&stateInfo,sizeof(StateInfo));
widget_tab_offline[0]->m_mutex_NTC.lock();

widget_tab_offline[0]->client.ntcSendMessageToServer((char  *)&ntcMsgSent, sizeof(NTCData));

qDebug() <<"debug---LED Color"<<sState<<"---fSpeed:"  <<  stateInfo.fSpeed  <<  "fLength:"  <<  stateInfo.fLength;

widget_tab_offline[0]->m_mutex_NTC.unlock();
```
NTC 缺陷调试菜单
```cpp
//NTC缺陷调试
connect(ui->actionNTC_Def, &QAction::triggered,[=](){
	DefectInfo stDefectInfo;
	QVariant Info;
	Info.setValue(stDefectInfo);
	emit m_pCommonCamera[1]->m_pPicHandle->sig_StoreData(Info);
});
```

发送登录消息 `NTC_MSG_LOGIN`
```cpp
widget_tab_offline[0]->NTC_SendToStr(NTC_MSG_LOGIN,UserName);
```
产品 ID=1 时通过 NTC 发串口消息
````cpp
#ifdef  NTC
	widget_tab_offline[iCamid-1]->NTC_SendToCom(sData);
#endif
``````

算法处理后向离线发瑕疵消息（NTC & 离线照片墙）
```cpp
#ifdef  NTC
	connect(m_pCommonCamera[i]->m_pPicHandle, SIGNAL(sig_StoreData(QVariant)), widget_tab_offline[i], SLOT(slot_SendData(QVariant)),Qt::UniqueConnection);
#endif
````
多处缺陷消息发送
````cpp
#ifdef  NTC
	//不存图时发,性能处理
	connect(m_pCommonCamera[i], SIGNAL(sig_StoreData(QVariant)), widget_tab_offline[i], SLOT(slot_SendData(QVariant)));
#endif
````
发送登录消息
```cpp
widget_tab_offline[0]->NTC_SendToStr(NTC_MSG_LOGIN,nullptr);
```
调用 `NTC_Camera_Init()` 初始化
````cpp
widget_tab_offline[0]->NTC_Camera_Init();
````
发送 EIP 状态（检测状态、踢废状态）
```cpp
#ifdef  NTC
if (m_bJSON_Control_Switch) //EIP开关
{
	for (int i =  0; i < m_iCamCount; i++)
	{
		//喷码
		if(i==0)//相机一 喷码
		widget_tab_offline[0]->NTC_SendToStr(NTC_MSG_EIP_STATU,"inkjet_kick_statu=1");
		//叠袋
		if(i==1)//相机二 叠袋
		widget_tab_offline[0]->NTC_SendToStr(NTC_MSG_EIP_STATU,"stacked_bag_kick_statu=1");
		//检测
		widget_tab_offline[0]->NTC_SendToStr(NTC_MSG_EIP_STATU,QString("CAM%1_detect_status=1").arg(i+1));
		//踢废
		widget_tab_offline[0]->NTC_SendToStr(NTC_MSG_EIP_STATU,QString("CAM%1_kick_status=1").arg(i+1));
	}
}
#endif
````

发送停机检测参数 `NTC_Detect_Shutdown()`
```cpp
#ifdef  NTC
// 控制台状态信息同步
if (m_pCommonCamera[0]->m_bChange_Coil  ==  false  && (m_pCommonCamera[0]->m_iCamCaptureNum  >  10  || m_iDisplayMode==2)) //卫材换卷后前十张不发状态
widget_tab_offline[0]->SendState();
#endif
```
构造 `DetectShutdown` 结构体，按 4 个分组填入缺陷米数，发送给控制台
````cpp
DetectShutdown ntc_detect_shutdown = {};
if(bDefect_Group_1 && fDefectCheck_mm_Group_1 > fDefectCheck_mm_Group_1_Last) // 一组
{
ntc_detect_shutdown.fDefectCheck_m_Group_1=fDefectCheck_mm_Group_1; // m
memcpy(ntc_detect_shutdown.reg_defects_Group_1, reg_defects_Group_1, sizeof(uint16_t)*32);
fDefectCheck_mm_Group_1_Last = fDefectCheck_mm_Group_1;
  
qDebug() <<  tr("第一组当前米数") << fCamera_mm_Group_1 <<  tr("查询米数") << fDefectCheck_mm_Group_1
<<  tr("停机缺陷") << strDefect_Group_1;
}
  
if(bDefect_Group_2 && fDefectCheck_mm_Group_2 > fDefectCheck_mm_Group_2_Last)
{
ntc_detect_shutdown.fDefectCheck_m_Group_2=fDefectCheck_mm_Group_2;
memcpy(ntc_detect_shutdown.reg_defects_Group_2, reg_defects_Group_2, sizeof(uint16_t)*32);
fDefectCheck_mm_Group_2_Last = fDefectCheck_mm_Group_2;
  
qDebug() <<  tr("第二组当前米数") << fCamera_mm_Group_2 <<  tr("查询米数") << fDefectCheck_mm_Group_2
<<  tr("停机缺陷") << strDefect_Group_2;
}
  
if(bDefect_Group_3 && fDefectCheck_mm_Group_3 > fDefectCheck_mm_Group_3_Last)
{
ntc_detect_shutdown.fDefectCheck_m_Group_3=fDefectCheck_mm_Group_3;
memcpy(ntc_detect_shutdown.reg_defects_Group_3, reg_defects_Group_3, sizeof(uint16_t)*32);
fDefectCheck_mm_Group_3_Last = fDefectCheck_mm_Group_3;
  
qDebug() <<  tr("第三组当前米数") << fCamera_mm_Group_3 <<  tr("查询米数") << fDefectCheck_mm_Group_3
<<  tr("停机缺陷") << strDefect_Group_3;
}
if(bDefect_Group_4 && fDefectCheck_mm_Group_4 > fDefectCheck_mm_Group_4_Last)
{
ntc_detect_shutdown.fDefectCheck_m_Group_4=fDefectCheck_mm_Group_4;
memcpy(ntc_detect_shutdown.reg_defects_Group_4, reg_defects_Group_4, sizeof(uint16_t)*32);
fDefectCheck_mm_Group_4_Last = fDefectCheck_mm_Group_4;
qDebug() <<  tr("第四组当前米数") << fCamera_mm_Group_4 <<  tr("查询米数") << fDefectCheck_mm_Group_4

<<  tr("停机缺陷") << strDefect_Group_4;
}

if (ntc_detect_shutdown.fDefectCheck_m_Group_1  >  0  ||
ntc_detect_shutdown.fDefectCheck_m_Group_2  >  0  ||
ntc_detect_shutdown.fDefectCheck_m_Group_3  >  0  ||
ntc_detect_shutdown.fDefectCheck_m_Group_4  >  0)
{

widget_tab_offline[0]->NTC_Detect_Shutdown(ntc_detect_shutdown); //发控制台

qDebug() <<  tr("停止状态，发送PLC查询消息!!!");

}
`````

发送相机状态 EIP 消息
```cpp
#ifdef  NTC
if (m_bJSON_Control_Switch) //EIP开关
{
	//相机灯
	int camera_statu=1;
	for (int i =  0; i < m_iCamCount; i++)
	{
		if(widget_cam_online[i]->m_cam_color  ==  1)
		{
			camera_statu=0;
			break;
		}
	}
	if(m_sCamera_statu!=camera_statu)
	{
		m_sCamera_statu=camera_statu;
		widget_tab_offline[0]->NTC_SendToStr(NTC_MSG_EIP_STATU,QString("camera_statu=%1").arg(m_sCamera_statu));
	}
}
#endif
````

## [Camera_Common_GigI.cpp]
密集缺陷检测过滤
```cpp
if(m_bDense_Filtering) //NTC收到密集缺陷消息置为true
{
m_bDense_Filtering =  false;
m_iCamDenseNum_p = m_iCamCaptureNum +  ceil(p_Mainwindow->m_fDense_Length/m_CamPara.iCamHeight*p_Mainwindow->m_calibration[m_CamPara.iCameraID-1].Y_Ratio/1000000.00);//获取不检测的最大下标id
}
````

发NTC串口消息
```cpp
QString sStr;
if (iHandle  ==  Handle_Kick) //2报警,贴标
{
	int  m_iControllerPassNum=0;//通道号
	int  SubCoilPos=p_Mainwindow->m_fComEdge;
	if(dInfo.Defect_X<=SubCoilPos)
	m_iControllerPassNum  =  0;
else
{
	for (int  i=0;i<Max_SubCoil;i++)
	{
		SubCoilPos+=g_SubCoilInfo_List[i+1];
		if(dInfo.Defect_X<=SubCoilPos) //判断区间
		{
		m_iControllerPassNum=i;
		break;
		}
	}
}
sStr +=  "10"; //起始位
sStr +=  QString("%1").arg(m_iControllerPassNum  /  8  +  1, 2, 16, QChar('0')); //站号 控制器号
sStr +=  "03ea"; //地址高位 低位
sStr +=  QString("%1").arg(m_CamPara.iKickID, 2, 10, QChar('0')); //kickid
sStr +=  QString("%1").arg(m_iControllerPassNum  %  8  +  1, 2, 16, QChar('0')); //踢废通道
sStr +=  QString("%1").arg(int(ceil((float)dInfo.Defect_H_Pix  /  m_CamPara.iCamHeight  *  255)), 2, 16, QChar('0')); //踢废长度
sStr +=  QString("%1").arg(int(ceil((float)dInfo.Defect_Y_Pix  /  m_CamPara.iCamHeight  *  255)), 2, 16, QChar('0')); //踢废位置
p_Mainwindow->SendToCom(m_CamPara.iCameraID,sStr);
iDefects_kick++;
if(iDefects_kick>=3&&m_iSaveImageMode  ==  Save_3_Defect) //踢三次
	break;
else  if(iDefects_kick>=10&&m_iSaveImageMode  ==  Save_Mix) //踢十次
	break;
else
	break;
}
else  if (iHandle  ==  Handle_Warning) //3报警,不贴标
{
p_Mainwindow->SendToCom(m_CamPara.iCameraID,"3F");
}
```

## dialog_template_management.cpp

````cpp
#ifdef  NTC
if (p_Mainwindow->widget_tab_offline[0]->m_bDebug  ==  false)
p_Mainwindow->widget_tab_offline[0]->SendLockState(false);
#endif
`````

## main.cpp

````cpp
#ifdef  NTC
	if(w.iStandAlone  ==  0)//0集成显示,不显示界面
	ShowWindow((HWND)w.winId(), SW_HIDE);
#endif
````
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTEzNDY3NzY3NTMsNDY3ODIyNTIwLC0yNj
cyNzU5NzEsLTYxNTc2MTQ0XX0=
-->